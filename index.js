


 /*for (let i = 0; i <= 10; i++) {
    const par = i % 2 === 0 ? 'par' : 'impar';
    console.log(`Progresso: ${i} esse número é ${par}`);
 } */

const frutas = ['maçã', 'banana', 'laranja', 'uva', 'abacaxi'];
// Usando for para iterar sobre o array de frutas

for (let i = 0; i < frutas.length; i++) {
    console.log(`Frutas: ${frutas[i]}`);
}